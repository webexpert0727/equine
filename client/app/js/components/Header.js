import React from 'react';
import HeaderComponent from '../containers/header-component';

const Header = () => (
    <div>
        <h2>Login</h2>
        <HeaderComponent />
    </div>
);

export default Header;
